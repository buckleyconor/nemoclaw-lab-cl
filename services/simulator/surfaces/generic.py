"""Generic REST surface — implemented in M8 for non-Redfish packs (laptop-fleet etc.).

Routes:
  GET /assets                        List all assets + state
  GET /assets/{asset_id}             Single asset + state
  GET /assets/{asset_id}/events      Active fault events
  GET /assets/{asset_id}/logs        Latest log lines
"""

# Placeholder — implement in M8 alongside the laptop-fleet pack.
