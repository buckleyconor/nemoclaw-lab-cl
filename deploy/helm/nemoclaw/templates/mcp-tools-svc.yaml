apiVersion: v1
kind: Service
metadata:
  name: {{ include "nemoclaw.fullname" . }}-mcp-tools
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "nemoclaw.labels" . | nindent 4 }}
    app.kubernetes.io/component: mcp-tools
spec:
  type: ClusterIP
  selector:
    {{- include "nemoclaw.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: mcp-tools
  ports:
    - name: http
      port: {{ .Values.mcpTools.port }}
      targetPort: http
      protocol: TCP
